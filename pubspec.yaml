name: dart_pusher_channels
description: Dart implementation of client of Pusher Channels protocol, instrument to work with real-time applications. 
version: 0.2.5

environment:
  sdk: '>=2.16.1 <3.0.0'

repository: https://github.com/mcfugger/dart_pusher_channels

platforms:
  android:
  ios:
  web:
  windows:

dependencies:
  http: ^0.13.2
  meta: ^1.7.0
  rxdart: ^0.27.3
  web_socket_channel: ^2.2.0

dev_dependencies:
  dart_code_metrics: ^4.16.0
  lints: ^1.0.0
  test: ^1.16.0
